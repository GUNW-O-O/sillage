/// 근접 검색의 조절값. 서버 액션과 입력 화면이 같은 값을 봐야 해서 따로 둔다
/// (`actions.ts` 는 "use server" 라 async 함수 말고는 export 할 수 없다).

/// 검색을 보내기 시작하는 최소 길이.
///
/// 트라이그램은 문자열을 3글자 조각으로 쪼개 그 조각을 가진 행을 꺼낸다. 질의가 짧으면
/// **조각이 적어 걸러낼 게 없다** — `'테'` 는 패딩까지 합쳐도 조각이 두어 개뿐이라
/// 그것을 포함한 행이 통째로 후보가 되고, 그 뒤 similarity 계산과 정렬이 후보 수만큼 돈다.
/// 인덱스가 일을 안 하는 게 아니라 인덱스가 좁힐 것이 없는 상태다.
///
/// 한 글자로는 어차피 원하는 것이 안 나오므로 사용자 체감으로도 손해가 없다.
export const MIN_QUERY_LENGTH = 2;
